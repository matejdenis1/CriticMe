import classes from "./Footer.module.css";
function Footer(){
    return(
        <footer className={classes.footer}>
            <p>Tento web slouží jako studenstký zápočtový projekt.</p>
        </footer>
    );

}
export default Footer;